<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/fragment_edit_category_layout_subCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/fragment_edit_category_subcategory_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Subcategory"
            android:layout_weight="1"
            android:textSize="16sp"
            android:padding="16dp"
            android:textStyle="bold" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/fragment_edit_category_switch_subcategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:layout_gravity="end"
            android:checked="true"
            app:thumbTint="@color/white_smoke"
            app:trackTint="@color/red"
            app:useMaterialThemeColors="false" />
    </LinearLayout>
    <!-- RecyclerView cho danh sách category -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fragment_edit_category_recycleView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="8dp"
        android:clipToPadding="false"
        android:overScrollMode="never" />
</LinearLayout>
